// this file is for including this library as code instead of as a dependency. This is useful for development and debugging purposes.
// used for development of HEE-DSSVue
includeBuild("$gradle.ext.externalLibDir") {
    dependencySubstitution {
        substitute(module("org.opendcs:usgs-waterdata-api"))
                .using(project(":"))
    }
}